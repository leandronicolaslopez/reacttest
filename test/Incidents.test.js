/**
 * @jest-environment node
 */

import Indicents from './../src/components/incidents/incidents';
import Confirmation from './../src/components/utils/confirmation/confirmation';

test('adds 1 + 2 to equal 3', () => {
    expect(3).toBe(3);
  });